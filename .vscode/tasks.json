{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate Nugget Package",
            "type": "shell",
            "command": "dotnet build && dotnet pack -c Release",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}